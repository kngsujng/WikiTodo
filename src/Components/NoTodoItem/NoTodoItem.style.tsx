import styled from 'styled-components';

export const NoTodoWrapper = styled.div`
	margin: 50% auto;
	text-align: center;
	color: ${({ theme }) => theme.txtColor};
	img {
		margin-bottom: 10px;
	}
`;
